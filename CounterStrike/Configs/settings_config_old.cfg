// THIS FILE IS FROM CSGO -- USE settings_config.cfg FOR A CURRENT CONFIG FOR CS2.

// Settings Config

// Type "exec settings_config" to run
// Includes non-graphics and controls related settings.
// Ensure it is saved as a ".cfg" file.
// Ensure "host_writeconfig" is at the end of the file.

// HUD

hud_scaling "0.85"
cl_hud_color "4"
cl_hud_background_alpha "0.5"

cl_hud_radar_scale "1"
cl_radar_scale "0.7"
cl_radar_always_centered "1"
cl_radar_rotate "1"
cl_radar_icon_scale_min "0.6"
cl_hud_bomb_under_radar "1"
cl_radar_square_with_scoreboard "1"

cl_hud_playercount_showcount "0"
cl_hud_healthammo_style "0"
cl_use_opens_buy_menu "1"
cl_showloadout "1"

// Inventory

cl_autowepswitch "0"
cl_sniper_delay_unscope "0"

closeonbuy "0"
cl_use_opens_buy_menu "1"

// Communication

cl_mute_enemy_team "0"
cl_mute_all_but_friends_and_party "0"

cl_hide_avatar_images "0"
cl_sanitize_player_names "0"
cl_sanitize_muted_players "0"

cl_show_clan_in_death_notice "0"
cl_teammate_colors_show "1"

// Disable game instructor

gameinstructor_enable "0"

// Disable audio when unfocussed

snd_mute_losefocus "1"

// Overwrite config

host_writeconfig
